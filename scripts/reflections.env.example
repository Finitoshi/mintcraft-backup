# Reflection Distribution Configuration
# Copy this to reflections.env and configure for your token

# Token mint address to distribute reflections for
MINT_ADDRESS=YourTokenMintAddressHere

# Path to treasury keypair (holds collected fees)
TREASURY_KEYPAIR_PATH=~/.config/solana/id.json

# Solana RPC URL
RPC_URL=https://api.devnet.solana.com

# Reward token mint (optional - if different from fee collection token)
# Leave empty or set to same as MINT_ADDRESS to distribute the same token
# Set to a different mint address to swap fees and distribute that token instead
REWARD_TOKEN_MINT=

# Swap slippage tolerance in basis points (100 = 1%)
# Only used if REWARD_TOKEN_MINT is different from MINT_ADDRESS
SWAP_SLIPPAGE_BPS=100

# Minimum holding to receive reflections (in base units)
# Example: 1000 tokens with 9 decimals = 1000000000000
MIN_HOLDING=1000000000000

# Wallets to exclude from reflections (comma-separated)
# Include: LP pools, CEX wallets, burn address, etc.
EXCLUDED_WALLETS=LPpoolAddress1,LPpoolAddress2,CEXwalletAddress

# Maximum distributions per run (prevents timeout)
MAX_DISTRIBUTIONS_PER_RUN=100

# Minimum total pool before distributing (prevents dust)
# Example: 1000 tokens with 9 decimals = 1000000000000
MIN_TOTAL_POOL=1000000000000

# Cron schedule (default: every hour)
# Format: minute hour day month weekday
# Examples:
#   0 * * * *    = Every hour
#   */30 * * * * = Every 30 minutes
#   0 */4 * * *  = Every 4 hours
CRON_SCHEDULE=0 * * * *
